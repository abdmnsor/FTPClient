<Window x:Class="FTPClient.FilesList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FTPClient"
        mc:Ignorable="d"
        Title="FilesList" Height="450" Width="800" Loaded="Window_Loaded">
    <Grid Grid.Row="1" Margin="0,0,0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="FTP Path:" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" x:Name="txtFtpPath" Text="/custemer_SEND" Margin="0,0,5,0" Padding="5"/>
            <Button Grid.Column="2" x:Name="btnBrowse" Content="Browse" Width="80" Click="btnBrowse_Click"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" x:Name="btnDownload" Content="Download Files" Height="40" Margin="0,0,5,0" Click="btnDownload_Click" Background="#007ACC" Foreground="White"/>
            
        </Grid>

        <!-- Directory Listing -->
        <GroupBox Grid.Row="1" Header="Directory Contents" Padding="10" Margin="0,0,0,10">
            <ListView x:Name="lstDirectoryContents" Height="250">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Type" Width="50" DisplayMemberBinding="{Binding Type}"/>
                        <GridViewColumn Header="Name" Width="300" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Size" Width="100" DisplayMemberBinding="{Binding Size}"/>
                        <GridViewColumn Header="DateTime" Width="310" DisplayMemberBinding="{Binding DateAndTime}"/>
                        <GridViewColumn Header="sortDate"  Width="0" DisplayMemberBinding="{Binding sortDate}" />
                    </GridView>
                </ListView.View>
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <EventSetter Event="Click" Handler="GridViewColumnHeader_Click"/>
                    </Style>
                </ListView.Resources>
            </ListView>
        </GroupBox>
    </Grid>
    
</Window>
